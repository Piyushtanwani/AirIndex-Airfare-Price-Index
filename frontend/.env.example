# Base URL of the AirIndex backend API. Defaults to http://localhost:8000
# when unset.
VITE_API_BASE_URL=http://localhost:8000

# Optional API key sent as the X-API-Key header on every request. Leave
# unset in development when the backend has API_KEY_REQUIRED=false.
VITE_API_KEY=
